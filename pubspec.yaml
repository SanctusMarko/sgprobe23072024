name: sgprobe23072024
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/SG.png

flutter_icons:
  ios: true
  image_path: "assets/icons/SG.png"

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/icons/SG.png
  branding: assets/icons/SG.png
  branding_mode: bottom
  branding_bottom_padding: 48
